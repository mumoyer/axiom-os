services:
  - type: web
    name: stage-gate-os
    env: node
    plan: starter
    buildCommand: npm ci && npm run build:client
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
