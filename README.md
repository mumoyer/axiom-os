# Stage Gate OS (stagegateos.com)

**The Autonomous Business Operating System with Deterministic Stage-Gate Verification.**

Inverting brittle AI venture failure modes into mathematical, anti-fragile guarantees with deterministic stage-gates, 100% full Git ejection, and a strict Zero-Charge Failure Guarantee.

---

## Core Architecture

1. **Tri-Plane Architecture:**
   - **Plane 1 (Cognitive Orchestrator):** Generates clean Next.js 15 + Tailwind + Supabase codebases.
   - **Plane 2 (Deterministic Stage-Gate Supervisor):** Enforces 5 automated verification hurdles (TypeScript AST, RFC 6125 TLS 1.3, Quad-DoH DNS Quorum, Stripe Payment Sandbox, and Clean-Room Git Ejection).
   - **Plane 3 (2PC Credit Escrow & Margin Guard):** Holds compute credits in cryptographic escrow, instantly refunding 100% upon unhealed failure (`ΔB == 0.00`).

2. **Zero-Charge Failure Guarantee:**
   - Platform absorbs compute COGS on failure retries.
   - Circuit breaker caps platform liability at $9.45 across 5 failures ($1.890 each).

3. **100% Dual-Push Full Git Ejection:**
   - Zero proprietary runtime lock-in (`@stagegate-os/proprietary-runtime` AST scan check).
   - Instant 1-click dual-push to founder GitHub/GitLab orgs.

---

## Quick Start

```bash
# Install dependencies
npm install

# Run development server
npm run dev

# Run full programmatic test suite (150+ tests)
npm test

# Run synthetic Playwright e2e checks (19 tests)
npm run test:e2e

# Build production bundle
npm run build && npm run build:client
```
